<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:ModManager.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="300"
        x:Class="ModManager.Views.SettingsWindow"
        x:DataType="vm:SettingsViewModel"
        Title="Settings" Width="1200" Height="300">

        <Grid RowDefinitions="40, 40, 40, 50" ColumnDefinitions="175, *, 75, 75" Margin="10">

            <TextBlock
                Text="Divinity Data Folder"
                Grid.Row="0"
                Grid.Column="0"
                Margin="5"
                VerticalAlignment="Center"
            />

            <TextBox
                Name="DataFolderTextbox"
                Text="{Binding DataDirectory}"
                Grid.Row="0"
                Grid.Column="1"
                Margin="5"
                VerticalAlignment="Center"
                TextChanged="DataFolderTextbox_TextChanged"
            />

            <Button
                Name="DataFolderButton"
                Content="Select"
                Grid.Row="0"
                Grid.Column="2"
                Margin="5"
                VerticalAlignment="Center"
                Command="{Binding SelectDataDirectory}"
            />

            <Button
                Name="AutoDataFolderButton"
                Content="Auto"
                Grid.Row="0"
                Grid.Column="3"
                Margin="5"
                VerticalAlignment="Center"
                Command="{Binding AutoSelectDataDirectory}"
            />

            <TextBlock
                Text="Current Profile"
                Grid.Row="1"
                Grid.Column="0"
                Margin="5"
                VerticalAlignment="Center"
            />

            <ComboBox
                Name="ProfileComboBox"
                ItemsSource="{Binding AllProfiles}"
                SelectedItem="{Binding CurrentProfile}"
                Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                VerticalAlignment="Center"
            />
            
            <TextBlock
                Text="Enable Dark Theme"
                Grid.Row="2"
                Grid.Column="0"
                VerticalAlignment="Center"
                Margin="5"
            />

            <CheckBox
                Grid.Row="2"
                Grid.Column="1"
                Margin="5"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                IsChecked="{Binding EnableDarkTheme}"
            />
            
            <Button
                Content="Apply"
                Name="ApplyButton"
                Click="ApplyButton_Click"
                Grid.Row="3"
                Grid.Column="0"
                VerticalAlignment="Bottom"
            />

            <Button
                Content="Cancel"
                Name="CancelButton"
                Click="CancelButton_Click"
                Grid.Row="3"
                Grid.Column="1"
                VerticalAlignment="Bottom"
            />
            
        </Grid>


</Window>